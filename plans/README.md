# Implementation Plans

Generated by the improve skill on 2026-06-14, against commit `e231628`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

These plans were written by an advisor model for execution by a separate,
zero-context executor. Each plan is self-contained.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Canonical killing-test fn name across all dispatch paths | P1 | S | — | DONE (worktree advisor/001-canonical-killing-test-name @ 019bfa8; not merged) |
| 002  | Cover all 8 op-class branches in self-test | P1 | S | — | DONE (worktree advisor/002-opclass-parser-coverage @ 673ffd5; not merged) |
| 003  | Fix README self-test count + dead codegraph link | P2 | S | — | DONE (worktree advisor/003-readme-doc-fixes @ 71dea91; not merged) |
| 004  | Add CI (self-test + shellcheck + JS check) | P2 | S | — (soft: 001, 002) | DONE (worktree advisor/004-ci-workflow @ 4e58323; not merged) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All four plans are independent and can land in any order; each touches a
  disjoint set of files (001: references + pipeline.js + self-test fixture name;
  002: self-test.sh new section; 003: README.md; 004: new .github/workflows/ci.yml).
- 004 has a **soft** dependency on 001 and 002: landing it last means CI exercises
  the unified test-fn naming and the new op-class coverage from the first green
  build. Not blocking — 004 works standalone.

## Notes on what was audited

Whole-repo, direct audit (no subagents — the repo is small: `SKILL.md`, four
`references/*.md`, three `scripts/*.sh`, one `workflow/pipeline.js`). Verification
baseline at plan time: `bash scripts/self-test.sh --quick` → `39 passed, 0 failed`.
The slow full `self-test.sh` (real `cargo mutants` crate smoke) and the live
agent-dispatch loop (Opus triage / Sonnet fix / cross-vendor audit) were **not**
exercised — the latter needs live models and is out of scope for static review.

## Findings raised but NOT turned into plans (by request)

- **#5 — Two dispatch paths duplicate their prompt/schema contracts and have
  drifted** (e.g. `cluster_members` in `ORCHESTRATION.md:143` vs
  `claimed_cluster_names` in `pipeline.js:80`). Real M-effort tech-debt, but its
  worst symptom (the test-fn naming divergence) is fixed by plan 001. Deferred,
  not rejected — revisit if the two paths drift further.

## Direction options surfaced (not problems; for the maintainer to weigh)

- Offline agent-loop smoke: a fixture mode feeding a canned `mko-grouped.json` and
  asserting the produced `findings.json` validates against the schema — would cover
  the biggest untested surface without live models.
- Extract `pipeline.js` pure helpers (`skepticPanel`, `slug`, caught/fake-kill set
  logic) into something `node`-testable; today only its syntax is checked.
- Decouple the engine roster from this machine (`pi`/`pior`/`~/.config/orcc/key`
  are hardcoded) via an `engines.local.json` indirection — only if this skill is
  ever shared beyond the personal setup.

## Findings considered and rejected

- The `rg -w '<fn>\s*\('` blast-radius ripgrep fallback (`IMPACT-RUBRIC.md:77`)
  looked suspect (`-w` wrapping a pattern that ends in `(`) but it is a documented
  best-effort heuristic behind a codegraph primary — not worth a plan.
- `verify-rerun.sh:97` treating empty `claimed_killed` as `missed` is by design
  (a finding that claims to kill nothing killed nothing), not a bug.
